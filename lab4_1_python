# Найти произведение двузначных нечетных чисел, кратных 13.
# Начальное значение произведения
k = 1
# Флаг для проверки, были ли найдены такие числа
flag = False
for num in range(10, 100):
    if num % 2 != 0 and num % 13 == 0: # Проверяем соответсвует ли условию
        k *= num  # Умножаем на текущее произведение
        flag = True  # Если числа найдены
# Если не найдено ни одного числа
if not flag:
    print("Нет двузначных нечетных чисел кратных 13.")
else:
    print("Произведение двузначных нечетных чисел кратных 13:", k)
