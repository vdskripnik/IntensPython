# Найти площадь кольца, внутренний радиус которого равен R1, а внешний радиус
# равен R2 (R1< R2).
# S=p(R**2-r**2), где R - внешний радиус, а r - внутренний.
import math
r=float(input('введите внутренний радиус:'))
R=float(input('введите внешний радиус:'))
if r<R:
    S=math.pi*(R**2-r**2)
    print(f'площадь кольца{S}')
else:
    print('ошибка,не удовлетворяет условию')
