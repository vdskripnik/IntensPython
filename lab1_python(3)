a = float(input("Введите длину стороны a (в метрах): "))
b = float(input("Введите длину стороны b (в метрах): "))
p = float(input("Введите плотность материала (в кг/м^3): "))

s = a * b # Вычисляем площадь

mass = s * p # Вычисляем массу

# Узнаем координаты центра масс
center_mass_x = a / 2
center_mass_y = b / 2

print(f"Масса пластины: {mass:.2f} кг")
print(f"Центр масс пластины: ({center_mass_x:.2f}, {center_mass_y:.2f}) м")
